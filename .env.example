# Environment configuration for Timberline integration testing

# OpenAI-Compatible LLM Configuration for AI analysis
OPENAI_API_KEY=your_openai_api_key_here   # Required for OpenAI provider
OPENAI_BASE_URL=                          # Custom endpoint for OpenAI-compatible APIs (optional)
OPENAI_MODEL=gpt-4o-mini                  # Model name or path to local model file
OPENAI_PROVIDER=openai                    # openai or llamacpp

# Logging Configuration (optional - defaults are set in docker-compose.yaml)
# LOGURU_LEVEL=INFO                       # DEBUG, INFO, WARNING, ERROR, CRITICAL (loguru standard)
# LOG_FORMAT=json                         # json or text
# LOGURU_FORMAT="{time} | {level} | {message}"  # Custom format string (text mode only)
