[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    -q
    --cov=analyzer
    --cov-report=term-missing
    --cov-report=html
asyncio_mode = auto
