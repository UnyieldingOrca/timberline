{{- if and .Values.attu.enabled .Values.attu.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: attu
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "timberline.attu.labels" . | nindent 4 }}
  {{- with .Values.attu.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.attu.ingress.className }}
  ingressClassName: {{ .Values.attu.ingress.className }}
  {{- end }}
  {{- if .Values.attu.ingress.tls }}
  tls:
    {{- range .Values.attu.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.attu.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: attu
                port:
                  number: {{ $.Values.attu.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
